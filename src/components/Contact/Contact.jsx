import React from 'react';

const Contact = (props) => {
    return (
        <div>
            <div>
                {props.title}
            </div>            
        </div>
    );
}

export default Contact;
